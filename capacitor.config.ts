import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shreeva.jaipurservicecompanycustomer',
  appName: 'JaipurServiceCompanyCustomer',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
